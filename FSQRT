#include <bits/stdc++.h>

#include<cmath>

using namespace std;


int main() {
    // your code goes here
    int t, n;
    cin >> t;
    while (t--) {
        cin >> n;
        int result = sqrt(n);
        cout << result << endl;
    }

}
